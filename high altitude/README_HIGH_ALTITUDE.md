🛰️ High Altitude (140m) Specialized Pipeline

140m 고도의 미세 객체(Small Object) 탐지 성능 극대화를 위한 특화 파이프라인입니다.

🚀 파이프라인 실행 순서

altitude_analysis.py: 객체 면적 기준 고도 분류 (Threshold: 0.0005).

Shuffle.py: Train/Val/Test 랜덤 분할 (Seed: 42).

data_preprocessing.py: 클래스 ID 단일화 (Bus, Truck -> 0: car).

data_preprocessing(high).py: 640px 정밀 슬라이싱 적용.

aerial_parking_detection(high).py: 모델 학습 수행.

💡 성능 향상을 위한 팁

파인튜닝 권장: 고고도 모델은 저고도 모델과 특징이 매우 다르므로, 별도의 모델로 독립적인 파인튜닝을 진행하는 것이 유리합니다.

데이터 증강: 작은 객체 탐지를 위해 학습 시 mosaic, mixup 파라미터를 적극 활용하십시오.

파라미터 우선 조정: 성능 변화를 주고자 할 때 모델 구조 변경보다는 imgsz와 슬라이싱의 overlap 비율을 먼저 조정하는 것을 추천합니다.

⚠️ 관리자 주의사항

경로 수정: 모든 .py 파일은 하드코딩된 경로를 포함하므로 사용자에 맞게 수정이 필수입니다.

슬라이싱 크기: 고고도 데이터는 반드시 640 이상으로 설정하여 미세 객체의 특징을 유지하십시오.